services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    restart: unless-stopped

    ports:
      - "3338:3010"
      - "3339:3010"

    environment:
      NODE_ENV: production
      PORT: 3010
      HOST: 0.0.0.0

    command: pm2-runtime start index.js -i 3